name: Workbench External Source Smoke

on:
  push:
    branches:
      - workbench-rework/audit-foundation
    paths:
      - "workbench/adapters/servers/**"
      - "workbench/migrations/**"
      - "cpp_api_index.py"
      - "cpp_dependency_index.py"
      - "build_integration_index.py"
      - "workbench_connect_server.py"
      - "test_fixtures/test_external_lsb_pipeline.py"
      - "test_fixtures/test_external_lsb_dsp_migration.py"
      - ".github/workflows/workbench-external-source-smoke.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  lsb-source-pipeline:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout Mission Toolkit
        uses: actions/checkout@v4
        with:
          path: toolkit

      - name: Checkout pinned LandSandBoat source
        uses: actions/checkout@v4
        with:
          repository: LandSandBoat/server
          ref: 3747feee0e38ab5c0283c4fe8deea7f0a9022351
          path: external/lsb
          sparse-checkout: |
            src
            cmake
            CMakeLists.txt
            sql
          sparse-checkout-cone-mode: false

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run external LSB analysis to canonical graph smoke test
        working-directory: toolkit
        env:
          PYTHONPATH: .
        run: python test_fixtures/test_external_lsb_pipeline.py ../external/lsb


  lsb-to-dsp-migration:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout Mission Toolkit
        uses: actions/checkout@v4
        with:
          path: toolkit

      - name: Checkout pinned LandSandBoat source
        uses: actions/checkout@v4
        with:
          repository: LandSandBoat/server
          ref: 3747feee0e38ab5c0283c4fe8deea7f0a9022351
          path: external/lsb
          sparse-checkout: |
            sql
          sparse-checkout-cone-mode: false

      - name: Checkout pinned archived DarkStar target
        uses: actions/checkout@v4
        with:
          repository: DarkstarProject/darkstar
          ref: ee1f489efbdee2d95a4f1a6c842790da9f54306e
          path: external/dsp
          sparse-checkout: |
            sql
          sparse-checkout-cone-mode: false

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Compare Excavation Duty logical migration slice
        working-directory: toolkit
        env:
          PYTHONPATH: .
        run: python test_fixtures/test_external_lsb_dsp_migration.py ../external/lsb ../external/dsp
