`<mission>.config.json` files go here if you use a package-building driver script that consumes
them (this project's own `package_mission.py` is a project-specific one-off, not bundled here).
Ships empty.
